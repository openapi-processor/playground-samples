openapi-processor-mapping: v7

options:
  package-name: io.openapiprocessor.generated

map:

  # path mappings apply only to the given endpoint
  paths:
    /parameter/add:
      parameters:
        - add: request => javax.servlet.http.HttpServletRequest
